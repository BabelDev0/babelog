---
interface Props {
  label: string;
  url: string;
}

const { label, url } = Astro.props;
---

<a
  href={url}
  class={`bg-black hover:scale-105 transition duration-300 text-white font-bold py-2 px-4 rounded
  text-5xl w-60 h-24 flex items-center justify-center font-flower`}
>
  {label}
</a>
